const databaseURL = 'https://hacker-news.firebaseio.com';
const apiVersion = '/v0';

export const apiURL = databaseURL + apiVersion;
export const newsURL = '/newstories.json';
export const itemURL = '/item/';
